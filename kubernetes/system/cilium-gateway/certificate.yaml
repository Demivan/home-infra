apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-home-demivan-me
  namespace: cilium-gateway
spec:
  secretName: wildcard-home-demivan-me-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.home.demivan.me"
