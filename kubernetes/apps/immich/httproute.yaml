apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: immich
  namespace: immich
spec:
  parentRefs:
    - name: internal
      namespace: cilium-gateway
  hostnames:
    - photos.home.demivan.me
  rules:
    - backendRefs:
        - name: immich-server
          port: 2283
