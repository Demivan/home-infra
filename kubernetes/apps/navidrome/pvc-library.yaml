apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: navidrome-library
  namespace: navidrome
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: navidrome-library
  resources:
    requests:
      storage: 100Gi
