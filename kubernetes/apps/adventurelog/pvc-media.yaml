apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adventurelog-media
  namespace: adventurelog
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
