apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: authentik
  namespace: identity
spec:
  parentRefs:
    - name: internal
      namespace: cilium-gateway
  hostnames:
    - auth.home.demivan.me
  rules:
    - backendRefs:
        - name: authentik-server
          port: 80
