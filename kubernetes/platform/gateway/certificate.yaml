apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-kube-demivan-me
spec:
  secretName: wildcard-kube-demivan-me-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.kube.demivan.me"
